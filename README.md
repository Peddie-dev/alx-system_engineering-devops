# Shell Variables and Expansions

This directory contains scripts related to shell variables, expansions, and aliases.

## Files

- **0-alias**: A script that creates an alias named `ls` with the value `rm *`.

## Usage

Make the script executable and source it to apply the alias:

```bash
chmod +x 0-alias
source ./0-alias
